query {
  events {
    id
    title
    start
    duration
  }
}

# will define mutation here

mutation CreateBooking($booking: SingleBooking!, $eventId: Integer!) {
  events (booking: $booking, eventId: $eventId) {
    id
    title
    start
    duration
  }
}